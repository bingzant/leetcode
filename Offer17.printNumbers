class Solution {
public:
    vector<int> printNumbers(int n) {
        int max = pow(10, n)-1;
        vector<int> result(max);
        for(int i=0; i<max; ++i) result[i]=i+1;
        return result;
    }
};
