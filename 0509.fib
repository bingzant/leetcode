class Solution {
public:
    int fib(int n) {
        if (n == 0 || n ==1) return n;
        return fib(n-1) + fib(n-2);
    }
};

/*
class Solution {
public:
    int fib(int n) {
        if (n <= 1) return n;
        int fn1 = 1, fn2 = 0;
        int fn;
        for (int i = 2; i <= n; i++) {
            fn = fn2 + fn1;
            fn2 = fn1;
            fn1 = fn;
        }
        return fn;
    }
};
*/
